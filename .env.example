# Public (safe for the browser). Required for authentication and data access.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server only. Do not prefix with NEXT_PUBLIC_. Not used by the foundation UI.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Server only. Required for the opportunity analyzer.
OPENAI_API_KEY=
AI_PROVIDER=mock